---
description: {{PROJECT_NAME}} 规则存放分层 — AI 如何找到规范
alwaysApply: true
---

# 项目规则放在哪

## 六层（不要全堆进 .cursor/rules）

| 层 | 位置 | 何时进上下文 | 放什么 |
|----|------|--------------|--------|
| **硬约束** | `.cursor/rules/*-gotchas.mdc` `alwaysApply: true` | 每会话 | ≤15 条必守 |
| **元规则** | 本文件 | 每会话 | 规则写哪、索引 |
| **按文件** | `.cursor/rules/*.mdc` + `globs:` | 编辑匹配文件时 | 如 `{APP_DIR}/**/*.tsx` |
| **协作宪章** | `docs/AI-COLLABORATION-CHARTER.md` | gotchas/AGENTS 索引；复杂任务时 Read | 通用协作方法论 |
| **专题规范** | `docs/*.md` | gotchas 点名或用户 `@` | 细则 + 脚本路径 |
| **活状态** | `docs/STATUS.md`、`docs/sessions/` | 新任务先读 STATUS | 进度，非永久规范 |
| **稳定需求** | `guidelines/` | 用户 `@` 或任务涉及 | 少改 |

## 新增一条项目规则

1. 3～10 条 bullet → 合适的 `.cursor/rules/*.mdc`
2. 全局可复用协作句 → `docs/AI-COLLABORATION-CHARTER.md`
3. 本项目路径表/案例 → `docs/MULTI-SOURCE-EXEC-PLAN.md` 等专题 + gotchas **一行链接**
4. 需步骤/示例/脚本 → `docs/主题-xxx.md` + gotchas **一行链接**
5. 一次性决策 → STATUS「最近决策」+ 可选 sessions
6. **同步** AGENTS「规则写在哪」+ gotchas 索引
7. **不要**指望「说过一次 AI 永远记得」——必须落盘

## 专题规范索引（按项目填写）

- 协作入口：`docs/STATUS.md`、`AGENTS.md`
- 复杂任务：`docs/COMPLEX-TASK-GATE.md`
- 多源计划：`docs/MULTI-SOURCE-EXEC-PLAN.md`
- 文档生命周期：`docs/DOC-LIFECYCLE.md`
- {{TOPIC_1}}：`docs/{{TOPIC_1_FILE}}.md`

决策树详见 project-context Skill → `references/rule-placement-decision-tree.md`
