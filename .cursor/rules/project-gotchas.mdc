---
description: 个人网站项目硬约束与常见坑点
alwaysApply: true
---

# 项目硬约束

- 新任务前先读 `docs/STATUS.md`、`docs/SITE-MANUAL.md`、`docs/AI-COLLABORATION-CHARTER.md` 和 `AGENTS.md`
- **禁止删改缩写** [`docs/AI-COLLABORATION-CHARTER.md`](../docs/AI-COLLABORATION-CHARTER.md) 核心章节；协作规则以宪章为完整权威源；详细计划在 `docs/sessions/`
- 改 `src/` 路由/页面/布局后 **同步更新 `docs/SITE-MANUAL.md`** 线框与路由表
- 用户说 **整理对话 / 归档语料 / 保存这次讨论** → 立即写入 `knowledge/raw/dialogues/`（用户全文 + AI 一行摘要）
- **新需求覆盖旧需求**：按 `docs/DOC-LIFECYCLE.md` + `docs/DOC-FRONTMATTER.md` — YAML `status` + 修订记录置顶（北京时间到秒）
- 对话归档：**Archie 原文完整**；**#00N 最新在上**；轮次从 1 递增（不用 transcript 行号）；实时取北京时间到秒
- **复杂任务**（架构/多模块/不可逆/需求含糊/跨会话整合）：按 **AI-COLLABORATION-CHARTER.md §一** 与 AGENTS 同节
- **复杂判断**（是否新页面、是否现在做某分支、技术选型）：选项 ≥2 + 可量化标准 + 推荐；见 AGENTS 同节
- 网站内容目标：**静态 build 读 md**；访客**只读**，无在线改仓库；`knowledge/raw/dialogues/` **已建**；`wiki/`、`content/` 延后（见 STATUS）
- 子任务不新建 `.cursor/plans/`；长方案写 sessions
- 根目录 `index.html` 是 React 入口：必须 `npm run dev`，双击会空白
- 设计探索改 `design-demos/`；功能实现改 `src/`
- 视觉对齐 benchmark：纸底 `#F6F0E2` / `#F5F0E8`，accent `#CC785C`
- 字体用 `fonts.loli.net`，勿直连 Google Fonts；`design-demos/logo-fonts/` 的 A/B/C 用本地 `fonts/` 自托管
- 搜索/下载 CDN 字体或素材若网络失败：**单独告知用户**（可开代理），勿静默交付「三栏看起来一样」的无效小样
- 勿提交 `node_modules/`、`dist/`、`.env`
- 不要擅自修改 `guidelines/` 除非用户明确要求
- 字幕排版规则见 `docs/SUBTITLE-FORMAT.md`（**一句一段** + 主题段；源稿更新跑 `node scripts/format-subtitles.mjs`）
